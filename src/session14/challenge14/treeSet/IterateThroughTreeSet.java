package session14.challenge14.treeSet;

import java.util.TreeSet;

public class IterateThroughTreeSet {

    public void iterateThroughTreeSet(TreeSet<String> treeSet) {
        for (String el : treeSet) {
            System.out.println(el);
        }
    }
}
